<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lagou.dao.UserMapper">
    
    <!--多条件分页查询-->
    <select id="findAllUserByPage" parameterType="com.lagou.domain.UserVo" resultType="com.lagou.domain.User">
        select * from user
        <where>
            <if test="true">
                and is_del != 1
            </if>
            <if test="userName != null and userName != ''">
                and name = #{userName}
            </if>
            <if test="startCreateTime != null and endCreateTime != null">
                and create_time between #{startCreateTime} and #{endCreateTime}
            </if>
        </where>
    </select>
    
    <!--更新用户状态-->
    <update id="updateUserStatus" parameterType="com.lagou.domain.User">
        update user set status = #{status}, update_time = #{update_time} where id = #{id}
    </update>
    
    <!--根据用户名查询用户-->
    <select id="login" resultType="com.lagou.domain.User" parameterType="com.lagou.domain.User">
        select * from user where phone = #{phone}
    </select>
    
    <!--根据用户id查询关联的角色信息-->
    <select id="findUserRoleById" resultType="com.lagou.domain.Role" parameterType="int">
        select * from roles r inner join user_role_relation ur on r.id = ur.role_id where ur.user_id = #{uid}
    </select>
    
    <!--根据角色id查询父级菜单信息-->
    <select id="findParentMenuByRoleId" resultType="com.lagou.domain.Menu" parameterType="list">
        select distinct m.*
        from roles r inner join role_menu_relation rm on r.id = rm.role_id
        inner join menu m on m.id = rm.menu_id
        where m.parent_id = -1
        and r.id in
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>
    
    <!--根据父级菜单查询子级菜单-->
    <select id="findSubMenuByPid" resultType="com.lagou.domain.Menu" parameterType="int">
        select * from menu where parent_id = #{pid}
    </select>
    
    <!--获取用户用户的资源权限信息-->
    <select id="findResourceByRoleId" resultType="com.lagou.domain.Resource" parameterType="list">
        select distinct r.*
        from resource r inner join role_resource_relation rr on r.id = rr.resource_id
        inner join roles ro on ro.id = rr.role_id
        where ro.id in
        <foreach collection="list" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>
    
    <!--根据用户id清空中间表-->
    <delete id="deleteUserContextRole" parameterType="int">
        delete from user_role_relation where user_id = #{userId}
    </delete>
    
    <!--为用户分配角色-->
    <insert id="userContextRole" parameterType="com.lagou.domain.User_Role_relation">
        insert into user_role_relation values (null, #{userId}, #{roleId},
        #{createdTime}, #{updatedTime}, #{createdBy}, #{updatedby})
    </insert>

</mapper>